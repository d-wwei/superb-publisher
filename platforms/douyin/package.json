{
  "name": "@social-cli/douyin",
  "version": "0.1.0",
  "description": "Douyin (TikTok China) browser automation CLI for publishing posts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "douyin-cli": "../../bin/douyin-cli"
  },
  "scripts": {
    "build": "tsup src/index.ts src/cli.ts --format cjs --dts --clean",
    "dev": "tsup src/index.ts src/cli.ts --format cjs --dts --watch"
  },
  "dependencies": {
    "@social-cli/core": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
