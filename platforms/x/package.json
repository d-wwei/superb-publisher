{
  "name": "@social-cli/x",
  "version": "0.1.0",
  "description": "X (Twitter) browser automation CLI for publishing tweets, threads, and articles",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "x-cli": "../../bin/x-cli"
  },
  "scripts": {
    "build": "tsup src/index.ts src/cli.ts --format cjs --dts --clean",
    "dev": "tsup src/index.ts src/cli.ts --format cjs --dts --watch"
  },
  "dependencies": {
    "@social-cli/core": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
